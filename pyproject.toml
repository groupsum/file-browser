[project]
name = "file-browser-workspace"
version = "0.1.0"
description = "SSOT-governed Groupsum file browser workspace."
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "ssot-registry>=0.2.24",
]

[tool.uv]
package = false

[tool.uv.workspace]
members = []
